# 第 50 天：异步数据库操作

## 学习目标

- 理解同步与异步的区别
- 掌握 SQLAlchemy 异步操作
- 学会使用 asyncpg / aiosqlite
- 理解连接池配置

---

## 1. 同步 vs 异步

### 同步问题

```python
# 同步：每个请求会阻塞一个线程
@app.get("/users/{user_id}")
def get_user(user_id: int):
    user = db.query(User).filter(User.id == user_id).first()
    return user
```

如果数据库查询需要 100ms，10 个并发请求就需要 10 个线程。

### 异步优势

```python
# 异步：协程切换，释放线程
@app.get("/users/{user_id}")
async def get_user(user_id: int):
    user = await db.execute(select(User).where(User.id == user_id))
    return user.scalar_one_or_none()
```

10 个并发请求可以共用 1 个线程。

---

## 2. SQLAlchemy 异步设置

### 安装依赖

```bash
pip install sqlalchemy[asyncio] asyncpg aiosqlite
```

### 异步 Engine

```python
from sqlalchemy.ext.asyncio import create_async_engine, AsyncSession
from sqlalchemy.orm import sessionmaker

DATABASE_URL = "sqlite+aiosqlite:///./app.db"
# PostgreSQL: "postgresql+asyncpg://user:pass@localhost/dbname"

async_engine = create_async_engine(
    DATABASE_URL,
    echo=True,
)

AsyncSessionLocal = sessionmaker(
    bind=async_engine,
    class_=AsyncSession,
    expire_on_commit=False,
    autocommit=False,
    autoflush=False,
)


async def get_db():
    async with AsyncSessionLocal() as session:
        yield session
```

### FastAPI 依赖注入

```python
from sqlalchemy.ext.asyncio import AsyncSession

async def get_db(db: AsyncSession = Depends(get_db)):
    yield db
```

---

## 3. 异步 CRUD

### 创建

```python
from sqlalchemy import select
from sqlalchemy.ext.asyncio import AsyncSession

async def create_user(db: AsyncSession, user_data: UserCreate):
    user = User(**user_data.model_dump())
    db.add(user)
    await db.commit()
    await db.refresh(user)
    return user
```

### 读取

```python
async def get_user(db: AsyncSession, user_id: int):
    result = await db.execute(select(User).where(User.id == user_id))
    return result.scalar_one_or_none()


async def get_users(db: AsyncSession, skip: int = 0, limit: int = 100):
    result = await db.execute(select(User).offset(skip).limit(limit))
    return result.scalars().all()
```

### 更新

```python
async def update_user(db: AsyncSession, user_id: int, user_data: UserUpdate):
    user = await get_user(db, user_id)
    if not user:
        return None

    for key, value in user_data.model_dump(exclude_unset=True).items():
        setattr(user, key, value)

    await db.commit()
    await db.refresh(user)
    return user
```

### 删除

```python
async def delete_user(db: AsyncSession, user_id: int):
    user = await get_user(db, user_id)
    if not user:
        return False

    await db.delete(user)
    await db.commit()
    return True
```

---

## 4. 关系加载策略

### selectinload（默认）

```python
from sqlalchemy.orm import selectinload

async def get_articles_with_author(db: AsyncSession):
    result = await db.execute(
        select(Article).options(selectinload(Article.author))
    )
    return result.scalars().all()
```

### joinedload（join 加载）

```python
from sqlalchemy.orm import joinedload

result = await db.execute(
    select(Article).options(joinedload(Article.author)).where(Article.id == 1)
)
```

### lazy / eager loading

```python
class Article(Base):
    # 默认 lazy load（需要时再查）
    author = relationship("User")

    # eager load（总是预加载）
    author = relationship("User", lazy="selectin")

    # 手动指定
    author = relationship("User", lazy="select")
```

---

## 5. 事务管理

### 自动提交

```python
async with AsyncSessionLocal() as session:
    async with session.begin():
        user = User(username="alice", email="alice@example.com")
        session.add(user)
        # 退出 with 时自动 commit
    # 如果异常，自动 rollback
```

### 手动控制

```python
async with AsyncSessionLocal() as session:
    await session.begin()

    try:
        user = User(username="alice", email="alice@example.com")
        session.add(user)
        await session.commit()
    except Exception:
        await session.rollback()
        raise
```

### savepoint（嵌套事务）

```python
async with AsyncSessionLocal() as session:
    async with session.begin_nested():
        session.add(User(username="alice", email="alice@example.com"))
    # 内层提交

    # 外层可以 rollback 整个事务
    # await session.rollback()
```

---

## 6. 连接池配置

```python
from sqlalchemy.ext.asyncio import create_async_engine

engine = create_async_engine(
    "postgresql+asyncpg://user:pass@localhost/dbname",
    pool_size=20,           # 初始连接数
    max_overflow=10,         # 额外连接数
    pool_timeout=30,         # 获取连接超时
    pool_recycle=3600,       # 连接回收时间（秒）
    echo=False,
)
```

---

## 实战练习

### 练习：异步 CRUD API

```python
# app/crud.py
from sqlalchemy import select
from sqlalchemy.ext.asyncio import AsyncSession
from typing import Optional
from app.models import User
from app.schemas import UserCreate, UserUpdate


async def create_user(db: AsyncSession, user: UserCreate) -> User:
    db_user = User(**user.model_dump())
    db.add(db_user)
    await db.commit()
    await db.refresh(db_user)
    return db_user


async def get_user(db: AsyncSession, user_id: int) -> Optional[User]:
    result = await db.execute(select(User).where(User.id == user_id))
    return result.scalar_one_or_none()


async def get_user_by_email(db: AsyncSession, email: str) -> Optional[User]:
    result = await db.execute(select(User).where(User.email == email))
    return result.scalar_one_or_none()


async def get_users(
    db: AsyncSession, skip: int = 0, limit: int = 100
) -> list[User]:
    result = await db.execute(select(User).offset(skip).limit(limit))
    return result.scalars().all()


async def update_user(
    db: AsyncSession, user_id: int, user: UserUpdate
) -> Optional[User]:
    db_user = await get_user(db, user_id)
    if not db_user:
        return None

    update_data = user.model_dump(exclude_unset=True)
    for key, value in update_data.items():
        setattr(db_user, key, value)

    await db.commit()
    await db.refresh(db_user)
    return db_user


async def delete_user(db: AsyncSession, user_id: int) -> bool:
    user = await get_user(db, user_id)
    if not user:
        return False

    await db.delete(user)
    await db.commit()
    return True
```

---

## 今日总结

- [ ] 同步阻塞线程，异步释放线程
- [ ] `create_async_engine` + `AsyncSession` 异步引擎
- [ ] `await db.execute(select(...))` 执行查询
- [ ] `lazy="selectin"` 预加载关系
- [ ] `session.begin()` 自动管理事务
- [ ] `pool_size` 控制连接池大小
- [ ] 生产环境用 PostgreSQL + asyncpg

---

*第 50 天 / 330 天*
*第二阶段：FastAPI 进阶 - 异步数据库*